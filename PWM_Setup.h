#pragma once

class PWM_Setup
{
   
  public:

  int Pin, Channel, Frequency, Resolution;
  
  PWM_Setup(int PWMPin, int PWMChannel, int PWMFrequency, int PWMresolution);

  private:

  protected:
    
};
